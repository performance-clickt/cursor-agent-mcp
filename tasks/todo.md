# tasks/todo.md — per-issue scratchpad

Local, disposable working state for the **one issue currently being executed**. Reset it
per issue. It is not a backlog — the backlog is the Linear project
[Cursor MCP](https://linear.app/clickt/project/cursor-mcp-f6d508e22579). The record of what
was planned and what shipped is the two Linear comments per issue (plan on start, wrap-up on
completion), not this file.

## Current issue

_None claimed yet._

Recommended first issue: **HM-557 — Verify GLM 5.2 is reachable via cursor-agent + add a model
preflight** (milestone M1 · Prove the path). It is the P0 that everything else depends on, and
it carries an open transport decision (see `docs/PROJECT.md` → Key decisions) that should be
resolved before implementation.

## Plan

- [ ] (fill with checkable steps when an issue is claimed)
